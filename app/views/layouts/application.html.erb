<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Library App" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Rails stylesheets and JavaScript -->
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Header -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
      <div class="container">
        <a class="navbar-brand" href="<%= books_path %>">Library System</a>
        <div>
          <%= link_to "Books", books_path, class: "btn btn-outline-light me-2" %>
          <%= link_to "Add Book", new_book_path, class: "btn btn-outline-light" %>
        </div>
      </div>
    </nav>

    <!-- Main Page Content -->
    <div class="container">
    
    <%= yield %>
      <% flash.each do |type, message| %>
  <div style=" max-width: 400px; margin: 12px auto; padding: 10px 16px; background-color: #16a34a; color: white;border-radius: 6px;font-size: 14px;text-align: center;">
    <%= message %>
  </div>
<% end %>


    </div>

    <!-- Footer -->
    <footer class="bg-light text-center py-3 mt-4">
      &copy; 2026 SuRakXyaa
    </footer>
  </body>
</html>
